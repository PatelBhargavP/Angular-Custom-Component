export enum RsplPositions {
    top = 'top',
    bottom = 'bottom',
    left = 'left',
    right = 'right'
}

export enum RsplDirections {
    start = 'start',
    center = 'center',
    end = 'end',
    below = 'below',
    above = 'above',
    before = 'before',
    after = 'after',
    ltr = 'ltr'
}