export enum TooltipPositions {
    top = 'top',
    bottom = 'bottom',
    left = 'left',
    right = 'right'
}

export enum TooltipDirections {
    start = 'start',
    center = 'center',
    end = 'end',
    below = 'below',
    above = 'above',
    before = 'before',
    after = 'after',
    ltr = 'ltr'
}